# В репозитории текстовые файлы хранятся с LF.
# Скрипты Windows всегда получают CRLF, скрипты POSIX — LF,
# независимо от настроек core.autocrlf на машине разработчика.
* text=auto
*.sh text eol=lf
mvnw text eol=lf
*.cmd text eol=crlf
*.bat text eol=crlf
